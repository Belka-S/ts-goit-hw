{
  "name": "ts-goit-hw",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "web-dev-server --node-resolve --watch",
    "dev": "tsc -w & nodemon dist/index.js &  web-dev-server --node-resolve --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@web/dev-server": "^0.4.1",
    "eslint": "^8.55.0"
  },
  "dependencies": {
    "nodemon": "^3.0.2"
  }
}
